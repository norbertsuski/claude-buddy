{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Grants the widget and settings windows the core event API for session snapshots and cursor positions, and window control for closing settings.",
  "windows": [
    "widget",
    "settings"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-close"
  ]
}

{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Grants the widget and settings windows the core event API for session snapshots and cursor positions, window control for closing settings, and notification delivery.",
  "windows": [
    "widget",
    "settings"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:window:allow-close",
    "notification:default"
  ]
}
